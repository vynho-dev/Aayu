/**
 * @startingPoint section="Components" subtitle="Label + value card — teal for money, ink for counts" viewport="700x140"
 */
export interface MetricCardProps {
  label: string;
  value: string;
  tone?: 'ink' | 'teal';
}
