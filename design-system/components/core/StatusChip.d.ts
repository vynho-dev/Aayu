/**
 * @startingPoint section="Components" subtitle="Success / attention / neutral pill status" viewport="700x100"
 */
export interface StatusChipProps {
  tone?: 'success' | 'attention' | 'neutral';
  children: React.ReactNode;
}
