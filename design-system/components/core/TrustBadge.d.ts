/**
 * @startingPoint section="Components" subtitle="Pill trust signal — encryption, pay-only-if-we-win" viewport="700x100"
 */
export interface TrustBadgeProps {
  /** Lucide icon name, e.g. 'shield-check' | 'lock' */
  icon?: string;
  children: React.ReactNode;
}
