Persistent pill signaling trust — always visible on upload + result screens.

```jsx
<TrustBadge icon="shield-check">Your data is encrypted</TrustBadge>
<TrustBadge icon="lock">Pay only if we win</TrustBadge>
```

Requires Lucide's UMD script + `lucide.createIcons()` call after mount (see Iconography guideline).
