/**
 * @startingPoint section="Components" subtitle="Icon + title + plain-language explanation" viewport="700x140"
 */
export interface AssessmentCardProps {
  icon?: string;
  title: string;
  children: React.ReactNode;
}
