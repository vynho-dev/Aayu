Document/letter card for the generated appeal — always carries "review before sending" (liability guardrail). With `editable`, the user can revise the letter inline before downloading the PDF (the locked appeal-letter decision).

```jsx
<LetterCard title="Appeal letter" editable preview="To the Claims Review Officer, ..." onDownload={dl} />
```
