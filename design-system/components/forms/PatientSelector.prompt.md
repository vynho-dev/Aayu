Row for picking or creating a patient (the parent) — sets context for the whole claim flow.

```jsx
<PatientSelector name="Appa" relation="Father" age={62} selected />
<PatientSelector name="Amma" relation="Mother" age={58} />
```
