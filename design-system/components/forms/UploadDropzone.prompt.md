Document upload zone with per-file status (reading / done / unreadable-retry).

```jsx
<UploadDropzone files={[{name:'rejection-letter.pdf', status:'done'}, {name:'policy.pdf', status:'reading'}]} onAdd={handleAdd} />
```
