/**
 * @startingPoint section="Components" subtitle="Bordered row — icon tile + title + subtitle + chevron" viewport="700x80"
 */
export interface ListRowProps {
  icon?: string;
  title: string;
  subtitle?: string;
  onClick?: () => void;
}
